# ------------------------------------------------------------------
# Frontend environment variables — copy to `.env` (or set in Vercel)
# ------------------------------------------------------------------

# Base URL of the deployed backend (Render). Leave empty in local dev —
# the Vite dev-server proxies /api to http://localhost:5000 automatically.
VITE_API_URL=https://your-backend.onrender.com
